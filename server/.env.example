# server port
PORT=4000

# JWT secret for auth tokens (choose a long random string)
JWT_SECRET=103856482011234567890987654321

# Admin passcode that grants access to admin endpoints
ADMIN_PASSCODE=stego

# Base URL of your deployed site (used for Stripe success/cancel)
# Example: http://localhost:4000 or https://your-app.onrender.com
BASE_URL=http://localhost:4000

# Stripe secret key (test mode)
STRIPE_SECRET_KEY=sk_test_stego1

# Stripe webhook signing secret (test mode) - set after creating webhook endpoint
STRIPE_WEBHOOK_SECRET=whsec_stego

# The price in cents (100 = $1.00). You can change if you wish.
PRO_PRICE_CENTS=100
